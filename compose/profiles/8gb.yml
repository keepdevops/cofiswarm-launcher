# 8 GB RAM profile: serverless RAG (sqlite-vec) + host-side infer (Metal).
# No in-container services beyond the base nats broker; gemma2b (scout) runs on
# host :8087 per infer-llama/profiles/gemma2b.env. RAG store is a local .db file.
services: {}
