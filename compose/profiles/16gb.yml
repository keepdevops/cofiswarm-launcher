# 16 GB RAM profile: RAG store + NATS bus + UI stub; host-side infer (Metal).
# Full server_groups run on host via cofiswarm-infer-* profiles.
services:
  pgvector:
    profiles: ["16gb"]
  nats:
    profiles: ["16gb"]
