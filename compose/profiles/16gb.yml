# 16 GB RAM profile: serverless RAG (sqlite-vec) + NATS bus + UI stub; host-side infer (Metal).
# Full server_groups run on host via cofiswarm-infer-* profiles.
services:
  nats:
    profiles: ["16gb"]
